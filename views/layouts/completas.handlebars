<main>
    <section class="tarefas">
        {{#each tarefas}}
        <div class="tarefa">
            <form action="">
                <div class="completa">
                    <input type="checkbox" id="tarefa-{{this.id}}" onchange="completarTarefa({{this.id}})">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div> 
            
                <button>
                    <img src="/imagens/x-icon.png" alt="ícone do X para excluir uma tarefa">
                </button>
            </form>
        </div>
        {{/each}}
            <div class="informacoes">
                <p>{{quatidadeTarefasAtivas}} tarefas ativas</p>
                
                <div class="botoes">
                    <a href="/" >Todas</a>
                    <a href="/ativas">Ativas</a>
                    <a href="/completas" class="active" >Completas</a>
                </div>

                <a href="">Limpar Tarefas</a>
            </div>
    </section>
//r
    <section class="botoes">
        <a href="/" >Todas</a>
        <a href="/ativas">Ativas</a>
        <a href="/completas" class="active" >Completas</a>
    </section>
</main>