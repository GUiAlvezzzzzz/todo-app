<main>
    <section class="tarefas">
        {{#each tarefas}}
        <div class="tarefa">
            <form action="">
                <div>
                    <input type="radio" id="tarefa-{{this.id}}">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>

                <button>
                    <img src="/imagens/x-icon.png" alt="ícone do X para excluir uma tarefa">
                </button>
            </form>
        </div>
        {{/each}}
            <div class="informacoes">

                <div class="botoes">
                    <a href="" class="active">Todas</a>
                    <a href="">Ativas</a>
                    <a href="">Completas</a>
                </div>

                <a href="">Limpar Tarefas</a>
            </div>
    </section>

    <section class="botoes">
        <a href="" class="active">Todas</a>
        <a href="">Ativas</a>
        <a href="">Completas</a>
    </section>

</main>